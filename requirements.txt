# Security & Cryptography
cryptography>=42.0.4
bcrypt>=4.0.0

# Google Cloud Platform
google-cloud-storage>=2.10.0
google-cloud-aiplatform>=1.40.0

# Machine Learning (Production - Python 3.14 Compatible)
# Using latest stable builds with pre-compiled wheels
pandas>=2.0.0
scikit-learn>=1.3.0

# Web Framework
flask>=2.3.0
jinja2>=3.1.0
requests>=2.31.0

# Optional: For advanced ML, use one of:
# - keras>=3.0.0 (works with Python 3.14)
# - torch (PyTorch - install separately as needed)
# - jax (Google's differentiable computing)
